services:
  ghosthub:
    build: .
    container_name: ghosthub
    restart: unless-stopped
    env_file: .env
    volumes:
      - ghosthub_data:/data
    expose:
      - "8000"
    networks:
      - app-network

volumes:
  ghosthub_data:

networks:
  app-network:
    external: true
